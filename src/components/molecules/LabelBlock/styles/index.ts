import styled from 'styled-components';

const Ul = styled.ul`
  align-items: center;
  display: flex;
  list-style-type: none;
  text-decoration: none;
  text-align: center;

  > li {
    margin-left: 10px;
  }
`;

export default Ul;
